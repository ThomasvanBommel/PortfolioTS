name: "Testing"
description: "Action to test PortfolioTS"

inputs:
  yt_api_key:
    description: "Youtube API key"
    required: true
    
runs:
  using: "docker"
  image: "./Dockerfile"
  args:
    - ${{ inputs.yt_api_key }}
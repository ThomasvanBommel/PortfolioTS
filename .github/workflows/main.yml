name: CI

on:
  push:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: dash

    steps:
      - name: Test
        run: ls -a

      - name: Build
        run: /root/run.sh


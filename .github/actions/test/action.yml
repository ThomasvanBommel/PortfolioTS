name: "Testing"
description: "Action to test PortfolioTS"
    
outputs:
  out:
    description: "script output"

runs:
  using: "docker"
  image: "../../../Dockerfile"
  env: 
    YT_API_KEY: ${YT_API_KEY}
    TEST: 12345